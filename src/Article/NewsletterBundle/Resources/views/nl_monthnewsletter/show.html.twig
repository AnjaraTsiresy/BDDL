{% extends '::base.html.twig' %}

{% block body -%}
    <h1>nl_monthnewsletter</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Monthnewsletter</th>
                <td>{{ entity.monthNewsletter }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nl_monthnewsletter') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nl_monthnewsletter_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
