<meta http-equiv="content-type" content="text/html; charset=utf-8"/>
<link rel="stylesheet" href="{{ pathToWeb }}/css/pdf1.css" type="text/css">
<style>
    .A4{
        background: white;
        height: 21cm;
        width: 29.7cm;
        display: block;
        margin: 0 auto;
        margin-top: 0.5cm;
    }

    @media print {
        body, .A4 {
            margin: 0;
            box-shadow: 0;
        }
    }
</style>

{%for i, dataTable in totaltableau %}

    {% if (tailleTable!=1 and tailleTable!=0) %}

        {% if (i==0) %}
            <!-- Affichage titre centré -->
            <page size="A4">
                <div class='conteneur'>
                    <div class='colonne1'>
                        <span style='position:absolute;
                              font-size: 10pt;
                              font-weight: bold;
                              left: 35%;
                              top: 50%;
                              width: 200px;
                              height: 200px;
                              margin-left: 0;
                              margin-top: 0; '>{{ title }}</span>
                        <div class='numpage1'> {{ numpage }}/{{ nb_page }}</div>
                    </div>
                    <div class='colonne2'>
                        <span style='position:absolute;
                              font-size: 10pt;
                              font-weight: bold;
                              left: 35%;
                              top: 50%;
                              width: 200px;
                              height: 200px;
                              margin-left: 0;
                              margin-top: 0; '> {{ title }} </span>
                        <div class='numpage2'>{{ numpage }} / {{ nb_page }}</div>
                    </div>
                </div>
            </page>
        {% endif %}

        <page size="A4">';
            <div class='conteneur'>
                <div class='colonne1'>

                    {% if (i==0) %}
                        <table class='titremangamanga'><tbody><tr><td><span> {{ title }} </span></td><td></td></tr></tbody></table>

                    {% endif %}



                    <div class='numpage1'>{{ numpage/nb_page }}</div>
                </div>
                <div class='colonne2'>
                    {% if (i==0) %}
                        <table class='titremangamanga'><tbody><tr><td><span>{{ title }}</span></td><td></td></tr></tbody></table>
                                        {% endif %}

                    <div class='numpage2'>{{ numpage }}/ {{ nb_page }}</div>
                </div>
            </div>
        </page>

    {% endif %}

{% endfor %}